machine:
  node:
    version: 7.4.0

dependencies:
  override:
    - yarn

test:
  override:
    - ./node_modules/.bin/gulp eslint
    - ./node_modules/.bin/gulp sasslint
    - ./node_modules/.bin/gulp test
