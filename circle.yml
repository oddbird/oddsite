machine:
  node:
    version: 6.2.2

dependencies:
  override:
    - npm install -g npm@3.10.2
    - npm install

test:
  override:
    - ./node_modules/.bin/gulp eslint
    - ./node_modules/.bin/gulp sasslint
