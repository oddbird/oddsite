machine:
  node:
    version: 7.2.1

dependencies:
  override:
    - npm install -g npm@4.0.5
    - npm install

test:
  override:
    - ./node_modules/.bin/gulp eslint
    - ./node_modules/.bin/gulp sasslint
